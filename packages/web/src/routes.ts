const routes = Object.freeze({
    HOME: { key: 'HOME', path: '/home', label: '홈' },
    LOGIN: { key: 'LOGIN', path: '/login', label: '로그인' },
    LOGOUT: { key: 'LOGOUT', path: '/logout', label: '로그아웃' },
    PORTFOLIO: { key: 'PORTFOLIO', path: '/portfolio', label: '내 포트폴리오' },
});

export default routes;
